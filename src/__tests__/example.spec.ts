/**
 * 示例测试 - 展示测试结构
 * 按照 TDD 规范，每个新功能对应一个 *.spec.js 或 *.spec.ts 文件
 */
import { describe, it, expect } from 'vitest'

describe('示例测试套件', () => {
  it('应该通过（占位用，新功能请在此目录添加对应测试文件）', () => {
    expect(true).toBe(true)
  })
})
