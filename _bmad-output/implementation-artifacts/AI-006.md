# Story 3.6: 自定义 API Headers

Status: review

## Story

As a DailyLogger 用户,
I want 自定义请求大模型 API 的 Headers,
so that 我可以使用各种 OpenAI 兼容的 API 服务（如 OpenRouter、Claude Code 等），这些服务需要特定的认证或标识 Headers。

## Acceptance Criteria

1. 用户可以在设置界面添加/编辑/删除自定义 Header（key-value 形式）
2. 系统预置常用 API 服务的 Header 模板（OpenRouter、Azure OpenAI、Claude Code 等）
3. 所有 LLM API 调用（截图分析、日报生成、周报/月报）自动携带自定义 Headers
4. Headers 存储在数据库中，敏感值可选择加密存储
5. 支持一键清除所有自定义 Headers
6. 提供 Header 预览功能，方便用户确认配置正确

## Tasks / Subtasks

- [x] Task 1: 后端数据模型扩展 (AC: 4)
  - [x] 1.1 在 Settings 结构体中添加 `custom_headers` 字段（JSON 格式：`[{"key": "X-Header", "value": "value", "sensitive": false}]`）
  - [x] 1.2 在 schema.rs 中添加数据库迁移（`ALTER TABLE settings ADD COLUMN custom_headers TEXT DEFAULT '[]'`）
  - [x] 1.3 更新 settings.rs 的 get_settings_sync 和 save_settings_sync 函数
  - [x] 1.4 编写单元测试验证序列化/反序列化

- [x] Task 2: 后端 API 调用逻辑修改 (AC: 3)
  - [x] 2.1 创建共享的 Header 构建函数（在 synthesis/mod.rs 或新建 utils 模块）
  - [x] 2.2 修改 synthesis/mod.rs 中的 `call_llm_api` 函数，应用自定义 Headers
  - [x] 2.3 修改 auto_perception/mod.rs 中的图片分析 API 调用，应用自定义 Headers
  - [x] 2.4 确保 Authorization Header 与自定义 Headers 合并逻辑正确
  - [x] 2.5 添加日志记录自定义 Headers（敏感值需 mask）

- [x] Task 3: 前端 UI 组件开发 (AC: 1, 2, 5, 6)
  - [x] 3.1 在 BasicSettings.vue 中添加"自定义 Headers"配置区块
  - [x] 3.2 实现 Header 列表展示（支持添加/编辑/删除）
  - [x] 3.3 实现预设模板下拉选择（OpenRouter、Azure OpenAI、Claude Code 等）
  - [x] 3.4 实现"清除所有"按钮
  - [x] 3.5 实现 Header 预览功能（显示所有已配置的 Headers）
  - [x] 3.6 添加 i18n 翻译（中文/英文）

- [x] Task 4: 集成测试与文档 (AC: 全部)
  - [x] 4.1 编写 Rust 集成测试验证 Header 应用
  - [x] 4.2 更新 architecture.md 文档
  - [x] 4.3 手动测试各预设模板的实际 API 调用

## Dev Notes

### 预设 Header 模板

```json
[
  {
    "name": "OpenRouter",
    "headers": [
      {"key": "HTTP-Referer", "value": "https://dailylogger.app", "sensitive": false},
      {"key": "X-Title", "value": "DailyLogger", "sensitive": false}
    ]
  },
  {
    "name": "Azure OpenAI",
    "headers": [
      {"key": "api-key", "value": "", "sensitive": true}
    ],
    "note": "api-key header 替代 Authorization header"
  },
  {
    "name": "Claude Code",
    "headers": [
      {"key": "anthropic-version", "value": "2023-06-01", "sensitive": false}
    ]
  }
]
```

### 数据结构设计

```rust
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CustomHeader {
    pub key: String,
    pub value: String,
    pub sensitive: bool,  // 是否敏感（需加密存储）
}

// Settings 新增字段
pub custom_headers: Option<String>,  // JSON: Vec<CustomHeader>
```

### 关键代码位置

- **Settings 结构体**: `src-tauri/src/memory_storage/mod.rs:26`
- **数据库迁移**: `src-tauri/src/memory_storage/schema.rs`
- **Settings 读写**: `src-tauri/src/memory_storage/settings.rs`
- **LLM API 调用**: `src-tauri/src/synthesis/mod.rs:45-144` (call_llm_api 函数)
- **图片分析**: `src-tauri/src/auto_perception/mod.rs` (analyze_screen_with_image 函数)
- **前端设置**: `src/components/settings/BasicSettings.vue`

### API 调用 Header 合并逻辑

```rust
// 1. 先设置 Content-Type
let mut request = client.post(&endpoint)
    .header("Content-Type", "application/json")
    .json(&request_body);

// 2. 设置 Authorization（如果 api_key 非空且没有自定义替代）
if !config.api_key.is_empty() {
    // 检查自定义 headers 是否已包含 Authorization 或替代 header
    let has_auth = custom_headers.iter().any(|h|
        h.key.to_lowercase() == "authorization" ||
        h.key.to_lowercase() == "api-key"
    );
    if !has_auth {
        request = request.header("Authorization", format!("Bearer {}", config.api_key));
    }
}

// 3. 应用自定义 headers
for header in custom_headers {
    request = request.header(&header.key, &header.value);
}
```

### Project Structure Notes

- 遵循现有 Settings 字段命名规范：snake_case
- JSON 序列化使用 serde_json
- 敏感 Header 值需要使用 crypto 模块加密存储
- 日志中敏感值使用 `mask_api_key` 函数处理

### References

- [Source: src-tauri/src/synthesis/mod.rs:45-144] - call_llm_api 函数
- [Source: src-tauri/src/memory_storage/settings.rs] - Settings 读写逻辑
- [Source: src/components/settings/BasicSettings.vue] - API 配置 UI
- [Source: architecture.md Section 5] - 数据库设计

## Dev Agent Record

### Agent Model Used

Claude Opus 4.6 (claude-opus-4-6)

### Debug Log References

- 2026-03-21: TypeScript type-check passed after fixing variable declaration order
- 2026-03-21: Clippy warnings fixed (collapsible_if, derivable_impls)

### Completion Notes List

- **Task 1**: Added `CustomHeader` struct with `key`, `value`, `sensitive` fields; Added database migration; Updated settings.rs with encryption/decryption for sensitive headers; Added 6 unit tests for serialization/deserialization
- **Task 2**: Updated `ApiConfig` struct with `custom_headers` field; Modified `call_llm_api` in synthesis/mod.rs to apply custom headers with Authorization fallback logic; Modified `analyze_screen` in auto_perception/mod.rs; Added logging with sensitive value masking
- **Task 3**: Added Custom Headers configuration section in BasicSettings.vue with preset templates, add/edit/delete functionality, clear all button, and header preview; Added i18n translations in en.json and zh-CN.json
- **Task 4**: Added 4 Rust integration tests for load_api_config with custom headers; Updated architecture.md with Section 9.4 documenting the feature; All 424 Rust + 915 frontend tests passing

### File List

**Modified Files:**
- `src-tauri/src/memory_storage/mod.rs` - Added CustomHeader struct and get_header_presets function
- `src-tauri/src/memory_storage/schema.rs` - Added custom_headers column migration
- `src-tauri/src/memory_storage/settings.rs` - Added encryption/decryption for sensitive headers
- `src-tauri/src/synthesis/mod.rs` - Added custom_headers to ApiConfig, applied headers in call_llm_api
- `src-tauri/src/auto_perception/mod.rs` - Added custom_headers to CaptureSettings, applied headers in analyze_screen
- `src/components/settings/BasicSettings.vue` - Added custom headers UI section
- `src/locales/en.json` - Added English translations for custom headers
- `src/locales/zh-CN.json` - Added Chinese translations for custom headers
- `_bmad-output/planning-artifacts/architecture.md` - Added Section 9.4 for custom headers documentation

## Change Log

- 2026-03-21: Initial implementation complete - all 4 tasks completed, all AC satisfied