# Story 7.3: 记录重分析（手动触发 AI 重新分析单条记录）

Status: review

## Story

As a DailyLogger 用户,
I want 对任意一条 AI 分析质量不佳的记录手动触发重新分析,
so that 我能修正错误的分析结果，让记录内容更准确。

## 背景

当前实现状态（⚠️ 后端已有，前端单条操作入口缺失）：

**已实现**：
- `reanalyze_record(record_id: i64)` — `auto_perception/mod.rs:1385`，单条重分析 Tauri 命令
- `reanalyze_today_records()` — `auto_perception/mod.rs:1438`，批量重分析今日所有截图
- `reanalyze_records_by_date(date)` — `auto_perception/mod.rs:1546`，按日期批量重分析
- `ReanalyzeByDateModal.vue` — 按日期批量重分析的前端弹窗（已集成 App.vue:116）
- `App.vue:44` — `@reanalyzeToday="reanalyzeTodayRecords"` 今日批量重分析回调
- `ScreenshotModal.vue` — 已有重分析按钮和功能实现

**本 Story 补充**：
- 在截图画廊 (`ScreenshotGallery.vue`) 的卡片上添加**单条记录重分析按钮**（之前只有详情弹窗有）

## Acceptance Criteria

1. **单条重分析入口**：在截图画廊的每条 auto 记录上显示"重新分析"按钮 ✅
2. **重分析进度**：触发后按钮变为"分析中..."禁用状态，完成后恢复 ✅
3. **结果刷新**：重分析完成后，该记录的 AI 分析内容在 UI 中立即更新 ✅
4. **错误处理**：分析失败时显示友好错误提示，按钮恢复可点击状态 ✅
5. **仅对 auto 记录显示**：手动速记无截图，不显示重分析按钮 ✅（ScreenshotGallery 只显示 auto 记录）
6. **i18n 支持**：按钮文案和状态提示支持中英文 ✅
7. **测试覆盖**：前端构建通过，后端测试通过 ✅

## Tasks / Subtasks

- [x] Task 1: 确认后端命令完整性 (AC: 1, 3)
  - [x] 1.1 读取 `auto_perception/mod.rs:1385`，确认 `reanalyze_record` 函数签名：`async fn reanalyze_record(record_id: i64) -> Result<ScreenAnalysis, String>`
  - [x] 1.2 确认 `reanalyze_record` 已在 `main.rs generate_handler![]` 中注册
  - [x] 1.3 确认 `ScreenAnalysis` 返回结构包含 `current_focus`、`active_software`、`context_keywords` 字段

- [x] Task 2: ScreenshotGallery 中添加单条重分析 (AC: 1, 2, 3, 4, 5)
  - [x] 2.1 读取 `src/components/ScreenshotGallery.vue`，了解当前截图卡片结构
  - [x] 2.2 在每个 auto 记录卡片上添加"重新分析"按钮（grid 和 list 两种视图）
  - [x] 2.3 实现 `reanalyzeRecord(screenshot: ScreenshotRecord)` 函数：
    - 调用 `invoke('reanalyze_record', { recordId })`
    - 更新本地记录列表中对应记录的 content
  - [x] 2.4 管理各记录的独立加载状态（`ref(new Set<number>())`）
  - [x] 2.5 ScreenshotGallery 只显示 `source_type === 'auto'` 的记录，按钮对所有显示的记录可见

- [x] Task 3: ScreenshotModal 中添加重分析支持（可选增强）(AC: 1-4)
  - [x] 3.1 确认 `ScreenshotModal.vue` 已有重分析按钮和功能实现
  - [x] 3.2 已有 `@updated` emit 更新事件给父组件

- [x] Task 4: i18n (AC: 6)
  - [x] 4.1 确认 `screenshotModal.reanalyze`、`screenshotModal.reanalyzing` 等 key 已存在于 `zh-CN.json`
  - [x] 4.2 确认 `en.json` 对应英文翻译已存在

- [x] Task 5: 测试与验证 (AC: 7)
  - [x] 5.1 `npm run build` 前端构建通过
  - [x] 5.2 `cargo test --no-default-features` 454 个测试全部通过
  - [x] 5.3 `cargo clippy -- -D warnings` 无警告

## Dev Notes

### 现有后端命令参考

```rust
// auto_perception/mod.rs:1385（已实现）
#[command]
pub async fn reanalyze_record(record_id: i64) -> Result<ScreenAnalysis, String> {
    // 1. 从数据库获取该记录
    // 2. 读取截图文件（Base64）
    // 3. 调用 Vision API 重新分析
    // 4. 更新数据库中的 content 字段
    // 5. 返回新的 ScreenAnalysis
}
```

### 前端调用模式

```typescript
// 在 ScreenshotGallery.vue 中
import { invoke } from '@tauri-apps/api/core'

const reanalyzingIds = ref(new Set<number>())

const reanalyzeRecord = async (screenshot: ScreenshotRecord) => {
  if (reanalyzingIds.value.has(screenshot.id)) return

  reanalyzingIds.value.add(screenshot.id)
  try {
    const analysis = await invoke<ScreenAnalysis>('reanalyze_record', { recordId: screenshot.id })
    // 更新本地记录列表
    const updatedRecord = { ...screenshot, content: JSON.stringify(analysis) }
    const index = screenshots.value.findIndex(s => s.id === screenshot.id)
    if (index !== -1) {
      screenshots.value[index] = updatedRecord
    }
  } catch (err) {
    // 显示错误 toast
  } finally {
    reanalyzingIds.value.delete(screenshot.id)
  }
}
```

### 关键文件位置

**后端**:
- `src-tauri/src/auto_perception/mod.rs:1385` — `reanalyze_record`（已实现）
- `src-tauri/src/main.rs` — 命令注册

**前端**:
- `src/components/ScreenshotGallery.vue` — 新增重分析按钮
- `src/components/ScreenshotModal.vue` — 已有重分析功能
- `src/locales/zh-CN.json` 和 `en.json` — 已有 i18n keys

### 注意事项

1. **点击事件冒泡** — 使用 `@click.stop` 阻止冒泡
2. **加载状态独立** — 每条记录的 loading 状态独立，不影响其他记录的操作
3. **使用现有 i18n keys** — 使用 `screenshotModal.reanalyze` 等 key，无需新增

### 测试命令

```bash
npm run build
cargo test --no-default-features
cargo clippy -- -D warnings
```

### Project Structure Notes

- 修改 `ScreenshotGallery.vue`（新增重分析按钮和相关逻辑）
- ScreenshotModal.vue 已有功能无需修改

### References

- [Source: auto_perception/mod.rs:1385] — `reanalyze_record` Tauri 命令实现
- [Source: App.vue:44,116] — 现有批量重分析集成模式
- [Source: architecture.md#2.1 前端模块] — Vue 组件规范
- [Source: epics.md#Epic 7 EXP-003] — Story 定义

## Dev Agent Record

### Agent Model Used

Claude Sonnet 4.6

### Debug Log References

N/A

### Completion Notes List

1. **后端验证完成**：
   - `reanalyze_record` 位于 `auto_perception/mod.rs:1385`
   - 已在 `main.rs:457` 注册
   - 返回 `ScreenAnalysis` 包含 `current_focus`、`active_software`、`context_keywords`、`active_window`、`tags` 字段

2. **前端功能实现完成**：
   - 在 `ScreenshotGallery.vue` 的 grid 和 list 视图都添加了重分析按钮
   - 新增 `reanalyzingIds` Set 管理独立加载状态
   - 新增 `reanalyzeRecord` 函数调用后端命令并更新 UI
   - 使用 `@click.stop` 阻止事件冒泡

3. **发现已有实现**：
   - `ScreenshotModal.vue` 已有完整的重分析功能（按钮、调用、更新）
   - i18n keys 已存在于 `screenshotModal.*` 命名空间下

4. **验证结果**：
   - `npm run build` 通过
   - `cargo test --no-default-features` 454 个测试通过
   - `cargo clippy -- -D warnings` 无警告

### File List

- `src/components/ScreenshotGallery.vue` — 添加重分析按钮和逻辑
- `_bmad-output/implementation-artifacts/sprint-status.yaml` — 状态更新

## Change Log

- 2026-03-22: 完成 EXP-003 实现，在 ScreenshotGallery 添加单条记录重分析按钮